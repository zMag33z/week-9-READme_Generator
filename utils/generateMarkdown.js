// Create a function that returns a license badge based on which license is passed in


// BEFORE THIS-- check to see if user input can be literal which would be awesome. 
              //OR at least enter img and links. Do away with some questions possibly...
// template 3 literals each side of code snippets
//```shell
//npm init -y
//```

// function booleans values of wether to add to document.



// If there is no license, return an empty string
function renderLicenseBadge(license) {
  // badge example
  // [![npm version](https://img.shields.io/npm/v/inquirer-press-to-continue)](https://npmjs.com/package/inquirer-press-to-continue)
}


// If there is no license, return an empty string
function renderLicenseLink(license) {

}

// If there is no license, return an empty string
function renderLicenseSection(license) {

}

// Generate Document with imported data.
function generateMarkdown(data) {
// Capitalized keys = title and subtitles.
// lowercase keys = info additions.
console.log(data);
//if data.contents === true  add to generate.
//code snippet block example
  return `
  ${data.title}





`;
}

module.exports = generateMarkdown;
